# Copy this file to `.env` for local development. Never commit a real `.env` file.
# Use `heuristic` for fully offline grading, or `openrouter`/`gemini` with a free API key.
AI_PROVIDER=heuristic
AI_API_KEY=
AI_MODEL=
